#
# Standard definitions for things.
#  
# Remove unconditionally + copy
RM	= rm -f
CP	= cp
#
# Directory stuff
#
MKDIR	= mkdir -p
RMDIR 	= rm -rf 
#
# Archives - always full compression.
# 
TAR	= tar
ZIP	= zip -9 
GZIP	= gzip -9 
#
# Install
#
INSTALL_FIL	= ginstall -m 0666  
INSTALL_DIR	= ginstall -m 0755 
INSTALL_EXE	= ginstall -m 0777
#
# Use microemacs for conversions.
#
ME      = me
DOS2UNIX= $(ME) -n "@dos2unix"
UNIX2DOS= $(ME) -n "@unix2dos"
